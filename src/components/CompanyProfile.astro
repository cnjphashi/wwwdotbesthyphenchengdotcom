---
import { companyProfile } from '../data/siteData';
---

<section id="company-profile" class="py-20 bg-slate-900">
  <div class="max-w-5xl mx-auto px-4 sm:px-6 lg:px-8">

    <div class="text-center mb-12">
      <h2 class="text-2xl md:text-3xl font-bold text-white mb-4">
        会社概要
      </h2>
      <p class="text-xl text-gray-300 max-w-2xl mx-auto">
        HATCATOYを運営する会社情報をご紹介します。
      </p>
    </div>

    <div class="bg-slate-900 rounded-xl border border-slate-700 overflow-hidden">

      {companyProfile.map((item) => (
        <div class="grid grid-cols-1 md:grid-cols-3 gap-4 px-6 py-6 border-b border-slate-700 last:border-b-0">

          <div class="text-white text-lg">
            {item.title}
          </div>

          <div class="md:col-span-2 text-gray-300 leading-relaxed">
            {
              Array.isArray(item.description)
                ? item.description.map((line) => (
                    <div>{line}</div>
                  ))
                : item.description
            }
          </div>

        </div>
      ))}

    </div>

  </div>
</section>
